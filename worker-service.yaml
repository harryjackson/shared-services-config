server:
  port: 10072
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10073
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
