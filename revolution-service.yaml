server:
  port: 10110
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10111
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
