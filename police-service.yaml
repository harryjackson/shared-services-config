server:
  port: 10352
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10353
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
