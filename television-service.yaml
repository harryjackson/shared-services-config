server:
  port: 10438
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10439
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
