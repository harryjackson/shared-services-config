server:
  port: 10092
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10093
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
