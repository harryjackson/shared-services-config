server:
  port: 10090
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10091
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
