server:
  port: 10006
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10007
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
