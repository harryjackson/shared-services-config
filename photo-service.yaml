server:
  port: 10346
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10347
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
