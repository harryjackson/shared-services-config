server:
  port: 10284
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10285
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
