server:
  port: 10368
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10369
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
