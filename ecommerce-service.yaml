server:
  port: 10022
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10023
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
