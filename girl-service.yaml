server:
  port: 10240
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10241
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
