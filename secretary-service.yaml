server:
  port: 10402
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10403
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
