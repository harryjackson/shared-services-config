server:
  port: 10008
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10009
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
