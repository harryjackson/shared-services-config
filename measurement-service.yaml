server:
  port: 10300
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10301
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
