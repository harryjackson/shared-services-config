server:
  port: 10098
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10099
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
