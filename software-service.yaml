server:
  port: 10416
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10417
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
