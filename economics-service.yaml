server:
  port: 10188
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10189
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
