server:
  port: 10070
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10071
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
