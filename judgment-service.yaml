server:
  port: 10280
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10281
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
