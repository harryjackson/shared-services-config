server:
  port: 10324
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10325
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
