server:
  port: 10326
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10327
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
