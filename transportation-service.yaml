server:
  port: 10444
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10445
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
