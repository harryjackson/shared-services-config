server:
  port: 10056
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10057
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
