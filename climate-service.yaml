server:
  port: 10130
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10131
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
