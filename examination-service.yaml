server:
  port: 10222
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10223
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
