server:
  port: 10274
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10275
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
