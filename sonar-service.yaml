server:
  port: 10064
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10065
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
