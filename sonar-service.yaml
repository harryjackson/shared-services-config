server:
  port: 10044
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10045
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
