server:
  port: 10036
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10037
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
