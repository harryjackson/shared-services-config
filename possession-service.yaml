server:
  port: 10356
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10357
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
