server:
  port: 10340
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10341
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
