server:
  port: 10256
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10257
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
