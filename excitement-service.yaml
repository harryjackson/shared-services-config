server:
  port: 10224
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10225
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
