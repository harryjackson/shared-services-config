server:
  port: 10428
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10429
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
