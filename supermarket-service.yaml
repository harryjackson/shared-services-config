server:
  port: 10168
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10169
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
