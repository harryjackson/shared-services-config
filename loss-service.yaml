server:
  port: 10294
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10295
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
