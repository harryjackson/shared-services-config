server:
  port: 10316
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10317
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
