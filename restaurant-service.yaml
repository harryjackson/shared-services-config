server:
  port: 10392
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10393
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
