server:
  port: 10174
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10175
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
