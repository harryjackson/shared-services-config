server:
  port: 10042
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10043
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
