server:
  port: 10078
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10079
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
