server:
  port: 10040
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10041
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
