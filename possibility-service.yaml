server:
  port: 10358
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10359
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
