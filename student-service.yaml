server:
  port: 10424
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10425
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
