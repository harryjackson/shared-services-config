server:
  port: 10030
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10031
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
