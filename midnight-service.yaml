server:
  port: 10304
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10305
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
