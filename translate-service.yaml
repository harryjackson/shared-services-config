server:
  port: 10068
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10069
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
