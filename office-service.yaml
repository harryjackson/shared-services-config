server:
  port: 10320
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10321
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
