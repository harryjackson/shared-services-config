server:
  port: 10404
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10405
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
