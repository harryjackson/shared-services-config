server:
  port: 10016
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10017
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
