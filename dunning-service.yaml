server:
  port: 10020
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10021
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
