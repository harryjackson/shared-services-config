server:
  port: 10162
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10163
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
