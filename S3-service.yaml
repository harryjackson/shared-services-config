server:
  port: 10000
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10001
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
