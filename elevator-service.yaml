server:
  port: 10128
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10129
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
