server:
  port: 10002
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10003
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
