server:
  port: 10010
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10011
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
