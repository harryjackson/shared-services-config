server:
  port: 10450
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10451
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
