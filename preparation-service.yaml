server:
  port: 10360
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10361
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
