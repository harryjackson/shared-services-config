server:
  port: 10096
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10097
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
