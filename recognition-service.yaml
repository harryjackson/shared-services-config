server:
  port: 10376
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10377
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
