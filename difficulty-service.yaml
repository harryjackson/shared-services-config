server:
  port: 10172
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10173
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
