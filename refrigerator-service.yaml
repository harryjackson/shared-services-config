server:
  port: 10380
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10381
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
