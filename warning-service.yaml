server:
  port: 10462
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10463
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
