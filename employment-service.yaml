server:
  port: 10182
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10183
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
