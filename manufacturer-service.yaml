server:
  port: 10298
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10299
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
