server:
  port: 10158
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10159
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
