server:
  port: 10244
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10245
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
