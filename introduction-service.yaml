server:
  port: 10082
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10083
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
