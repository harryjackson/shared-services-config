server:
  port: 10062
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10063
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
