server:
  port: 10062
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10063
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
