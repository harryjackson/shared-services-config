server:
  port: 10100
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10101
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
