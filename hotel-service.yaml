server:
  port: 10254
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10255
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
