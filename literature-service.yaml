server:
  port: 10292
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10293
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
