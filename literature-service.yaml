server:
  port: 10140
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10141
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
