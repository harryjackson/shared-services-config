server:
  port: 10084
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10085
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
