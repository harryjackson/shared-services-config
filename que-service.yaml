server:
  port: 10058
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10059
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
