server:
  port: 10328
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10329
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
