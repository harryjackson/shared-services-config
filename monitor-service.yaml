server:
  port: 10046
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10047
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
