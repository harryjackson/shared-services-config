server:
  port: 10400
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10401
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
