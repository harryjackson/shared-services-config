server:
  port: 10112
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10113
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
