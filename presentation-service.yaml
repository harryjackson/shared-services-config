server:
  port: 10142
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10143
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
