server:
  port: 10364
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10365
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
