server:
  port: 10420
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10421
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
