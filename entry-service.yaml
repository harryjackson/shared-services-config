server:
  port: 10210
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10211
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
