server:
  port: 10038
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10039
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
