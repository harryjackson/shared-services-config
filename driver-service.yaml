server:
  port: 10184
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10185
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
