server:
  port: 10252
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10253
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
