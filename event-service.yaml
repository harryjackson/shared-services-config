server:
  port: 10218
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10219
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
