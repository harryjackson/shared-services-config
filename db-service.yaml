server:
  port: 10018
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10019
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
