server:
  port: 10094
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10095
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
