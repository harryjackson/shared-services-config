server:
  port: 10190
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10191
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
