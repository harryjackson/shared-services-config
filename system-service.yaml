server:
  port: 10430
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10431
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
