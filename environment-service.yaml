server:
  port: 10192
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10193
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
