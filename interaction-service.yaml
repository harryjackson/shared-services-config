server:
  port: 10272
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10273
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
