server:
  port: 10266
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10267
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
