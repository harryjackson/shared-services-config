server:
  port: 10314
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10315
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
