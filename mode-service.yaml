server:
  port: 10306
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10307
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
