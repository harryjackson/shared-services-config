server:
  port: 10080
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10081
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
