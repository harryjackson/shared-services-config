server:
  port: 10296
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10297
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
