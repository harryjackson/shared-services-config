server:
  port: 10032
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10033
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
