server:
  port: 10216
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10217
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
