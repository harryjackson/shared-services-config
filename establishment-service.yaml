server:
  port: 10138
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10139
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
