server:
  port: 10196
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10197
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
