server:
  port: 10004
  tomcat:
    threads:
      max: 20

management:
  server:
    port: 10005
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
