server:
  port: 10024
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10025
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
