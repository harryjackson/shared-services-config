server:
  port: 10418
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10419
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
