server:
  port: 10076
  tomcat:
    threads:
      max: 2

management:
  server:
    port: 10077
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
